<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.dao.testDao">
    <!-- 查询广告 -->
    <select id="selectTestMap" parameterType="java.lang.String" resultType="java.util.Map">
        SELECT * FROM tt_circle WHERE id = #{id}
    </select>

    <!-- 查询广告 -->
    <select id="selectTestList" parameterType="java.lang.String" resultType="java.util.Map">
        <!--        SELECT * FROM advertising-->
        SELECT * FROM 前端用户 WHERE 账号名 = '荷花'
    </select>

    <!-- test -->
    <!--    <select id="selecttest" parameterType="java.lang.String" resultType="java.util.Map">-->
    <!--        SELECT * FROM advertising-->
    <!--    </select>-->

</mapper>